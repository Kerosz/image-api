describe("Image Service", function () {
  describe("resizeImage func", function () {
    it("should return a string with the processed image path", function () {
      expect(true).toBeTruthy();
    });

    it("should return a cached path if the image has been already processed", function () {
      expect(true).toBeTruthy();
    });
  });
});
